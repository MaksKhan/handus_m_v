#include <iostream>
#include <vector>
#include <map>
#include <set>
#include <algorithm>
int main()
{
    int n;
    std::cin >> n;
    std::string s;
    for (int _ = 0; _ < n; _++) {
        std::vector<char> v;
        std::cin >> s;
        for (int i = 0; i < s.size(); i++) {
            v.push_back(s.at(i));
        }
        
    }
    

}
